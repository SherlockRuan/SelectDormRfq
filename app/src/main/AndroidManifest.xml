<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.example.leisu.selectdorm">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="26" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <meta-data
        android:name="android.support.VERSION"
        android:value="26.0.0-alpha1" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SelectWay">
        </activity>

        <activity android:name=".StudentInfo">
        </activity>

        <activity android:name=".DormInfo">
        </activity>

        <activity android:name=".SelectDorm">
        </activity>

        <activity android:name=".SelectOne">
        </activity>

        <activity android:name=".SelectFinal">
        </activity>

        <activity android:name=".SelectTwo">
        </activity>

        <activity android:name=".SelectThree">
        </activity>

        <activity android:name=".SelectFour">
        </activity>

    </application>

</manifest>
